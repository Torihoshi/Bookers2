<h1><%= @user.name %>の詳細</h1>

<%= render 'user', user: @user %>

<h2>投稿一覧</h2>

<% @user.books.each do |book| %>
  <div class="book">
    <h3><%= book.title %></h3>
    <p><%= book.body %></p>
    <%= link_to "詳細", book_path(book) %>
  </div>
<% end %>