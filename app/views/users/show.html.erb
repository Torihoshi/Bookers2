<p>users/show</p>
<!--左部分-->
<!--ユーザー表示機能-->
<h2>User info</h2>
<%= render 'user', user: @user %>

<!--新規投稿機能-->
<h2>New book</h2>
<%= render 'books/new', book: @book_new %>

<!--右部分-->
<h2>投稿一覧</h2>

<% @user.books.each do |book| %>
  <div class="book">
    <h3><%= book.title %></h3>
    <p><%= book.body %></p>
    <%= link_to "詳細", book_path(book) %>
  </div>
<% end %>